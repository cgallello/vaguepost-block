{
  "name": "vagueblock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:fixture": "node tests/fixture-smoke.mjs",
    "lint": "node lint.mjs",
    "check": "node --check background.js && node --check content.js && node --check classifier.js && node --check shared/policy.js && node --check shared/policy.mjs && node --check shared/strike.mjs && node --check popup.js && node --check options.js && node --check package-extension.mjs && node --check release-audit.mjs && npm run lint",
    "package": "npm test && npm run check && node package-extension.mjs",
    "release-check": "npm run package && node verify-package.mjs",
    "release-audit": "node release-audit.mjs",
    "release-audit:strict": "node release-audit.mjs --strict",
    "eval:example": "node eval/score.mjs eval/dataset.example.json eval/predictions.example.json"
  }
}
